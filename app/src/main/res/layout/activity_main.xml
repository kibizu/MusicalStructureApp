<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <LinearLayout

        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/tan_background"
        android:orientation="vertical"
        tools:context=".MainActivity">

            <include android:id="@+id/tool_bar"
                layout="@layout/tool_bar"/>


        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="7"
            android:layout_marginTop="@dimen/margin16"
            >
            <ImageView
                android:layout_width="@dimen/dimension500"
                android:layout_height="@dimen/dimension407"
                android:src="@drawable/giradischi"/>
        </RelativeLayout>


        <include android:id="@+id/tracks_e_favorite"
            layout="@layout/tracks_e_favorite" />

        <include android:id="@+id/playlist_e_recent"
            layout="@layout/playlist_e_recent" />

    </LinearLayout>
</layout>